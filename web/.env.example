# The frontend talks ONLY to the .NET backend (no demo layer).
# Copy to .env.local and point at your running MyAi.Api (launchSettings pins http://localhost:5000).
NEXT_PUBLIC_API_BASE=http://localhost:5000
